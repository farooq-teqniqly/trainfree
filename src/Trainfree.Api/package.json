{
  "name": "trainfree-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply programs_db --local"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
