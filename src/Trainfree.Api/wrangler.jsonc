{
    "name": "trainfree",
    "compatibility_date": "2026-08-03",
    "d1_databases": [
        {
            "binding": "DB",
            "database_name": "programs_db",
            // Placeholder -- replace with the real ID from a one-time `wrangler d1 create
            // programs_db` run (see CLAUDE.md: resource creation is a separate, non-idempotent
            // setup script, never part of the deploy pipeline). Not required for
            // `--local` migrations/dev, which use a local sqlite file regardless of this value.
            "database_id": "00000000-0000-0000-0000-000000000000"
        }
    ]
}
