{
  "name": "trainfree-identity-api",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "powershell -NoProfile -ExecutionPolicy Bypass -File ../../scripts/Kill-Port.ps1 -Port 9998",
    "dev": "wrangler dev",
    "test": "vitest run",
    "provision": "node scripts/provision-identity.js",
    "smoke-check": "node smoke-harness/check.js"
  },
  "dependencies": {
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.0.0"
  }
}
