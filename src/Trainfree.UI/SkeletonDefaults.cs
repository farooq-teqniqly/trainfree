namespace Trainfree.UI;

/// <summary>
/// Shared default values for skeleton loading placeholders.
/// </summary>
public static class SkeletonDefaults
{
    /// <summary>
    /// Gets the default number of placeholder rows to render while a table's initial
    /// fetch is in flight.
    /// </summary>
    public const int RowCount = 4;
}
